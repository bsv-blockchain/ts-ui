{
  "name": "@bsv/ts-ui",
  "private": true,
  "description": "BSV TypeScript UI monorepo — React components and web apps for the BSV stack",
  "scripts": {
    "build": "pnpm -r run build",
    "test": "pnpm -r run test",
    "lint": "pnpm -r run lint",
    "clean": "pnpm -r run clean"
  },
  "engines": {
    "node": ">=20",
    "pnpm": ">=9"
  },
  "packageManager": "pnpm@9.0.0"
}
